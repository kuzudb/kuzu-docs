---
---
<script is:inline>
  window.addEventListener('DOMContentLoaded', () => {
    const banner = document.getElementById('top-banner');
    const closeButton = document.getElementById('close-banner');
    if (!banner || !closeButton) return;
    if (localStorage.getItem('kuzuBannerClosed_v2') !== 'true') {
      banner.classList.remove('hidden');
    }
    if (localStorage.getItem('kuzuBannerClosed_v2') === 'true') {
      banner.style.display = 'none';
      return;
    }
    closeButton.addEventListener('click', () => {
      banner.style.display = 'none';
      localStorage.setItem('kuzuBannerClosed_v2', 'true');
    });
  });
</script> 

<div class="sl-banner hidden" id="top-banner">
  <div class="banner-content">
    <a href="https://forms.gle/hHbooDCWNQnx56ve8" target="_blank" rel="noopener noreferrer" class="banner-link">
      <span class="enterprise-waitlist-text">
        🙏 Help us improve by filling out this survey →
      </span>
    </a>
  </div>
  <button id="close-banner" aria-label="Close banner">&times;</button>
</div>

<style>
  .hidden { 
    display: none !important; 
  }

  .sl-banner {
    background-color: rgba(22, 173, 249, 0.3);
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding: 0.25rem 2.5rem;
    position: sticky;
    color: white;
    font-size: 0.875rem;
    top: 0;
    z-index: 100;
    gap: 1rem;
    width: 100%;
    min-height: 2.5rem;
  }
  .banner-content {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
  }
  .banner-link {
    display: flex;
    align-items: center;
    justify-content: center;
    color: inherit;
    text-decoration: none;
    font-weight: 500;
    min-width: 0;
    text-align: center;
  }
  .banner-link:hover {
    text-decoration: underline;
  }
  .enterprise-waitlist-text {
    color: var(--sl-color-text);
    font-size: 0.875rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 100%;
    display: inline-block;
    margin-left: 0.5em;
  }
  @media (min-width: 768px) {
    .enterprise-waitlist-text {
      font-size: 0.875rem;
    }
  }
  @media (prefers-color-scheme: dark) {
    .enterprise-waitlist-text {
      color: var(--sl-color-text);
    }
  }
  #close-banner {
    background: none;
    border: none;
    color: var(--sl-color-gray-3);
    font-size: 1rem;
    cursor: pointer;
    align-self: center;
    margin-left: 0.5rem;
  }
  #close-banner:hover {
    color: var(--sl-color-white);
  }

</style>

